# Python
__pycache__/
*.pyc
.venv/
venv/

# local cache DB (regenerated by ingest.py — no need to version it)
backend/storage/*.sqlite

# secrets (added in Phase 3 for Azure keys)
.env

# frontend (added in Phase 5)
node_modules/
dist/
